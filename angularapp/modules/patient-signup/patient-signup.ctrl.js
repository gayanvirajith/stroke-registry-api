'use strict';

var app = angular.module('stroke-registry.patientSignup');


app.controller("patientSignupController",[
  '$location', 
  '$scope', 
  '$http', 
  '$sanitize',
  'flash', 
  'authService',
  '$mdToast',
  function($location, $scope, $http, $sanitize, flash, authService) {

}]);